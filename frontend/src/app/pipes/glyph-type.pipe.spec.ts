import { GlyphTypePipe } from './glyph-type.pipe';

describe('GlyphTypePipe', () => {
  it('create an instance', () => {
    const pipe = new GlyphTypePipe();
    expect(pipe).toBeTruthy();
  });
});

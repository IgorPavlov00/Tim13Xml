import { TrademarkTypePipe } from './trademark-type.pipe';

describe('TrademarkTypePipe', () => {
  it('create an instance', () => {
    const pipe = new TrademarkTypePipe();
    expect(pipe).toBeTruthy();
  });
});
